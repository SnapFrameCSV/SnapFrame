# LOG (append-only; newest at the bottom)

Format: `YYYY-MM-DD HH:MM Sydney | run type | what I did | what I saw | escalate?`

2026-09-11 10:30 Sydney | interactive | Phase 1 assessment delivered; Gate 0 cleared with "A" | — | no
2026-09-11 11:45 Sydney | interactive | Phase 2 design delivered; Gate 1 approved unchanged | — | no
2026-09-11 12:10 Sydney | interactive | Scaffolded repo (docs, STATE, LOG, DECISIONS, LICENSES, runbook, gate-02); no GitHub access in this session ("No linked GitHub account") | Proxy injects GitHub credentials once the account is linked, so no token is ever needed in chat | gate-02
2026-09-11 12:20 Sydney | interactive | gate-02 cleared: GitHub linked as SnapFrameCSV; routine "Snapframe operate loop" created (daily 17:00 UTC = 03:00 Sydney); scaffold sent to it as a fire-payload bundle because the interactive session has no repo binding | api.github.com/user → 200; repos/SnapFrameCSV/snapframe → 403 "not enabled for this session" | no
2026-09-11 15:30 Sydney | interactive | Routine created from the Cowork session ran (50 s) but the repo stayed empty: sessions and routines started from this chat are bound to no repository. Deleted that routine. Wrote gate-02b: operator uploads the folder via GitHub's web page and creates the routine in the app with the repo selected | api.github.com/repos/SnapFrameCSV/snapframe → 403 "not enabled for this session" from the interactive session | gate-02b
2026-09-11 (scheduled) | routine | loop proof run OK — gate-02b cleared, added `.github/workflows/ci.yml` (web upload skipped the dot-folder as expected), recorded network facts in OPERATIONS.md | repo GET → 200, `npm view esbuild version` → 0.28.2 (registries reachable in this environment) | no
2026-09-11 16:30 Sydney | routine | Step-0 repair: moved `snapframe-upload/` to the repo root, dropped top-level duplicates, removed redundant `scripts/ci-workflow.yml` (tripped CI's secret grep), added D14. Wrongly concluded from a stale fetch that run 1 never reached `main`; sent a false "LOOP NOT PROVEN" notification, then verified with `git ls-remote` that `main` already had run 1's commits, withdrew D15, corrected STATE/gate-02b/OPERATIONS and sent a correction | repo GET → 200; esbuild 0.28.2; origin/main = run 1's `a10b21b` before this run | no (correction notification sent)
2026-09-11 16:40 Sydney | routine (build) | Step-0 repair again (operator upload `f252d87` left two files in `snapframe-upload/agent/`; kept root ROUTINE-PROMPT.md per step 0, moved automerge-workflow.yml to `agent/`, flagged the differing step 4b in STATE.md). Then BUILD Slice 1: `extension/` skeleton (manifest, commands, settings, esbuild bundle, node:test ×4, generated icon, README/CHANGELOG/telemetry.json), 5 dev deps in LICENSES.md, `extension` CI job | direct push to `main` accepted twice; CI on `main` green for all prior commits; local typecheck/test/package green; esbuild 0.28.2 | one question in STATE.md (which routine prompt to use) — no reply needed unless the new one was intended
2026-09-11 17:10 Sydney | routine (build) | Confirmed run 3's CI green on both jobs, no open Issues. BUILD Slice 2 sub-step 1: capture command — pure text transforms (`dedent`/`expandTabs`/`softWrap`) with 9 new tests, `resolveCaptureSource` (selection or whole file), `runCapture` (clipboard-HTML paste-capture technique via a webview, clipboard restore, plain `<pre>` fallback), new `snapframe.wrapColumn` setting, wired `snapframe.capture` off its stub | typecheck clean; 13/13 tests pass; `vsce package` produced a valid `.vsix`; hygiene grep and preflight green; no new deps | no
2026-09-11 (scheduled) | routine (build) | Confirmed run 4's CI green on both jobs, no open Issues/PRs. BUILD Slice 2 sub-step 2 part 1: frame chrome — pure `buildFrameSvg` (background solid/gradient, padding, shadow, corner radius, window-control dots, title bar with escaped file name, foreignObject for the measured content) with 9 new tests; `readFrameSettings` config reader; rewired the preview webview to measure captured content, ask the host to build the SVG with live settings, and re-render on `snapframe.*` config changes | typecheck clean; 22/22 tests pass; `vsce package` produced a valid `.vsix`; hygiene grep and preflight green; no new deps | no
2026-09-11 (scheduled) | routine (build) | Confirmed run 5's CI green on both jobs, no open Issues/PRs. BUILD Slice 2 sub-step 2 part 2: PNG export — pure `buildExportFileName` (4 new tests) plus `readExportSettings`; preview webview gained an Export PNG button that rasterises the frame SVG via `Image`/canvas/`toBlob` at `snapframe.scale`×, optionally tries an image clipboard write first, then posts the PNG bytes + clipboard outcome to the extension host, which saves via `exportFolder` or `showSaveDialog` and reports the result in the status bar (never a silent text fallback); added `img-src blob: data:` to the panel CSP | typecheck clean; 26/26 tests pass; `vsce package` produced a valid `.vsix`; hygiene grep and preflight green (grep hit only inside gitignored `extension/node_modules`, confirmed clean excluding it, matching what CI's hygiene job actually scans); no new deps | no
2026-09-11 (scheduled) | routine (build) | Confirmed run 6's CI green on both jobs, no open Issues/PRs. BUILD Slice 2 sub-step 2 remainder: line numbers — `buildFrameSvg` draws a numbered gutter sized to the last line's digit count when `snapframe.lineNumbers` is on (3 new tests); `capture/source.ts` reports the raw selection's line span, `capture/panel.ts` threads it through so the webview counts displayed rows correctly for the HTML path (raw span, never re-wrapped) vs. the plain-text fallback (its own line count, since soft-wrap can add rows) — resolves the alignment caveat flagged in runs 5–6. Also fixed a real bug in run 6's dims regex: an un-doubled `\s` inside the webview's outer TS template literal is a silently-dropped non-escape character, so the shipped regex could never match real SVG output and the Export PNG button could never appear in a real VS Code host; never caught by CI since nothing here runs the webview JS in a browser | typecheck clean; 29/29 tests pass; `vsce package` produced a valid `.vsix`; hygiene grep and preflight green (checked with node_modules excluded); no new deps | no
2026-09-11 (interactive, same session) | routine (build) | Slice 8: `docs/03-launch-readiness.md` (built pieces, test evidence, CI/golden/dry-run links, listing preview, pending sandbox transcript, post-gate wiring table, §9 checklist, go-live steps, limitations); publish.yml dry run dispatched; Phase 3 build complete, budget reverts to 10-minute operate runs | docs | next human action: gate 03
2026-09-11 (interactive, same session) | routine (build) | Slice 7: `publish.yml` (tag/dry-run, trusted publishing or VSCE_PAT, Open VSX when OVSX_PAT), `metrics.yml` + `scripts/marketplace-metrics.mjs` (vsce-style extensionquery, tolerant parse, keeps last snapshot on error; 3 tests), Better Stack step in gate-05, OPERATIONS tables filled | 108/108 tests; Slice 5 CI + deploy-worker (skipped deploy) green | no
2026-09-11 (interactive, same session) | routine (build) | Slice 6: gate files 03 (Marketplace publisher), 04 (Stripe, test mode first), 05 (Cloudflare + signing key, two replies), 06 (Open VSX, optional), 07 (go live); D18 sets the hand-over order 03→05→04→06→07 | docs only | gates not yet handed over — the interactive session does that one at a time after Slice 7
2026-09-11 (interactive, same session) | routine (build) | Slice 5: `worker/` — WebCrypto Ed25519 minting in the extension's key format (interop proven against the extension's verify algorithm), Stripe read-only client, webhook signature check, routes /health /buy /success /webhook /lost-key /stats with KV; 16 node:test tests with injected KV/fetch/clock; key-pair generator page; `worker` CI job + `deploy-worker.yml` that skips deploy until gate-05 | 16/16 worker tests; typecheck clean; zero runtime deps | no
2026-09-11 (interactive, same session) | routine (build) | Slice 4 item 10 (Slice 4 closed): batch export of all selections / all open editors via awaited quick sessions with a folder override; Markdown image link after export (pure helper); `commands.ts` + contract test pinning the free tier's five commands | 105/105 tests; typecheck clean; no new deps | no
2026-09-11 (interactive, same session) | routine (build) | Slice 4 item 9: terminal capture via `workbench.action.terminal.copySelection` + clipboard round-trip, pure `prepareTerminalText` (ANSI strip, trim, tabs, wrap), `startCapture` accepts a plain-text source; CI green on `909b726` | 99/99 tests; typecheck clean; no new deps | no
2026-09-11 (interactive, same session) | routine (build) | Slice 4 item 8: `buildMultiFrameSvg` (N cards side-by-side/stacked with labels; single-card output byte-identical), `captureBefore`/`captureAfter` Pro commands, two layout settings | 95/95 tests; typecheck clean; no new deps | no
2026-09-11 (interactive, same session) | routine (build) | Slice 4 item 7 (D16): in-house QR encoder (v1–10, EC M, no runtime dep) verified by decoding with dev-only `jsqr`; QR drawn as vector squares in the band under the card; two settings; README/design table updated | 91/91 tests; typecheck clean; one new dev dep (jsqr) in LICENSES.md | no
2026-09-11 (interactive, same session) | routine (build) | Slice 4 item 6: highlight bands, focus-dim, callout pills (Pro) with a pure `parseLineRanges`; four new settings; presets learn highlightColor/focusDim | 86/86 tests; typecheck clean; no new deps | no
2026-09-11 (interactive, same session) | routine (build) | Slice 4 item 5: gradient angle + multi-stop, data-URI background image (host reads the file, 5 MB cap), caption/brand colour band — all behind a pure gate that collapses to the free defaults; free output byte-identical | 80/80 tests; typecheck clean; no new deps | no
2026-09-11 (interactive, same session) | routine (build) | Slice 4 item 4: named presets (Pro) — pure validated JSON module (`presets/presets.ts`, 8 tests), five gated commands (save/apply/delete/export/import) over globalState | 74/74 tests; typecheck clean; no new deps | no
2026-09-11 (interactive, same session) | routine (build) | Slice 4 items 2–3: WebP + PDF export (dependency-free PDF writer with SMask), transparent background (Pro, gated in `readFrameSettings`), 3×/4× scale (gated in `readExportSettings`); CI three-OS matrix confirmed green on `8f80bcb` | 66/66 tests; typecheck clean; no new deps | no
2026-09-11 (interactive, same session) | routine (build) | Slice 4 item 1: SVG export (Pro) — pure `export/formats.ts` gate with D05 contract tests, generic `saveExport`, `export-svg` message, Pro-only toolbar button | 58/58 tests; typecheck clean | no
2026-09-11 (interactive, same session) | routine (build) | Slice 3: real Ed25519 `verifyKey` (SPKI from raw key, signature checked before payload parse, 24 h future-iat tolerance, plain-English failure reasons), `publicKey.ts` placeholder, `initialiseLicence`/`enterLicence` with SecretStorage + "Snapframe Pro" status bar, `isPro()` driven by the verified key; 12 licence tests with a per-run key pair | 51/51 tests; typecheck clean; no new deps | no
2026-09-11 (interactive, same session) | routine (build) | Closed Slice 2 sub-step 4: extension CI job is now an ubuntu/windows/macos matrix running the golden suite (package/upload Linux-only); added an activation-time test (bundle + stubbed `vscode`, `activate()` < 200 ms, registered commands = manifest) | 41/41 tests; typecheck clean | no
2026-09-11 (interactive, same session as run 9) | routine (build), operator asked to keep going | Fixed Export PNG: `buildFrameSvg` now draws the code as native SVG `<text>`/`<tspan>` runs instead of a `<foreignObject>`; webview extracts per-line styled runs plus font/colour/background from the measured DOM; card uses the captured editor background; `webview-shell.test.ts` is now a real golden suite (full pipeline → PNG with correct IHDR size at 1×/2× and sampled pixel colours) | typecheck clean; 40/40 tests pass; `vsce package` valid; hygiene and preflight green; no new deps | no
2026-09-11 (scheduled) | routine (build) | Confirmed run 8's CI green on both jobs, no open Issues/PRs. BUILD Slice 2 sub-step 4 part 1: extracted the webview shell into a pure `webview/shell.ts`, added Playwright, and loaded the real webview JS in headless Chromium for the first time — found Export PNG (button and quickSnap alike) fails in a real browser with a tainted-canvas error, because `buildFrameSvg`'s `<foreignObject>` unconditionally taints any canvas it's rasterised into (a ~2023 browser hardening change, isolated with a standalone repro, not sandbox-specific). Landed 4 new tests: 2 cover what works (paste-fallback/measure, svg-message/button-visibility), 2 are named KNOWN BUG and pin today's actual `export-failed` behaviour so the fix has a regression test to flip | typecheck clean; 35/35 tests pass; `vsce package` produced a valid `.vsix`; hygiene grep and preflight green; CI now installs Playwright's Chromium | escalated in STATE.md "Next run should" #3 as top priority — no operator action needed, nothing is published
2026-09-11 (scheduled) | routine (build) | Confirmed run 7's CI green on both jobs, no open Issues/PRs. BUILD Slice 2 sub-step 3: `snapframe.quickSnap` — same capture→measure→SVG→rasterise pipeline in a throwaway webview panel that auto-exports as soon as its SVG arrives, is disposed before any save dialog, and returns focus to the editor (documented why: no offscreen webview API, no canvas in the extension host, and the paste-capture itself needs a focused webview); `capture/panel.ts` refactored to per-webview `Session` objects so a quick snap never touches an open preview; new `export-failed` message so a failed export surfaces as an error, not just panel text; wired the command off its stub. Also added a source-level test that fails on any single-backslash escape inside the webview `<script>` (the run-7 bug class), mutation-checked. CHANGELOG/README stop claiming nothing captures yet | typecheck clean; 31/31 tests pass; `vsce package` produced a valid `.vsix`; hygiene grep and preflight green (node_modules excluded); no new deps | no
