# STATE

**Read this first on every run. Update it before every run ends.**

## Current phase
Phase 3 — Build. Slice 1 (`extension/` skeleton) landed; Slice 2 sub-step 1 (capture command, plain preview) landed; sub-step 2 (frame chrome, PNG export, line numbers) landed; sub-step 3 (`snapframe.quickSnap`) now landed too. Next: sub-step 4 (golden-image suite), which closes out Slice 2. Nothing is published; nothing customer-facing exists.

## Blocked on
Nothing.

## Last run
- 2026-09-11 (scheduled routine, run 8): **Slice 2, sub-step 3: `snapframe.quickSnap`.** Confirmed run 7's CI was green in both jobs on `main` (`45ed463`) before starting, no open Issues, no open PRs. Approach decided and documented (in the `runQuickSnap` doc comment): VS Code has no offscreen webview API, the extension host has no `Image`/`canvas`, and the clipboard-HTML capture itself already needs a *focused* webview for its paste event — so "no panel" cannot mean "no webview". Quick Snap therefore runs the identical pipeline (paste-capture → measure → `buildFrameSvg` → rasterise) inside a throwaway panel that auto-exports the moment its SVG arrives (the host sends `autoExport: true` on the `svg` message), then the host disposes the panel *before* any save dialog so it is on screen as briefly as possible, saves via the same `exportPng` path (`exportFolder` or `showSaveDialog`, same status-bar outcomes), and hands focus back to the original editor/selection via `showTextDocument`. "Last settings" = the live `snapframe.*` configuration, which is what the preview persists to. The open preview panel (if any) is never touched. `capture/panel.ts` refactored from module-level `activePanel`/`lastCapture` singletons to a per-webview `Session` object (`{ panel, quick, capture, returnTo }`) so a preview session and a quick-snap session can coexist; the preview remains the singleton that re-renders on settings changes. Webview also gained an `export-failed` message (host shows an error and, in quick mode, closes the panel and returns focus) instead of only writing the reason into the panel's status line. `extension.ts` wires `snapframe.quickSnap` to `runQuickSnap` (off its "not available" stub); `enterLicence`/`buyPro` stay stubs. Also acted on run 7's request to audit the webview `<script>` for other silently-eaten single backslashes: found none by hand, then added `src/test/webview-escapes.test.ts` — a source-level `node:test` that walks every backslash in the inline script block and fails on any that is not doubled (or an outer-literal `` \` ``/`\$` escape), plus a positive check that the dims regex and the `'\\n'` split are doubled. Mutation-checked: un-doubling the dims regex makes both tests fail. `CHANGELOG.md` 0.0.1 entry and the README "preview build" note updated to stop claiming nothing captures yet (0.0.1 is unreleased, so nothing customer-facing changed). Not unit-testable here, same as runs 4–7: the webview/dispose/focus flow needs a real VS Code host — that is what sub-step 4's headless-Chromium suite starts to cover for the webview half. No new dependencies (`LICENSES.md` unchanged). Verified locally: `tsc --noEmit` clean, 31/31 tests pass (29 prior + 2 new), `vsce package` still produces a valid `.vsix` (9 files, same cosmetic `../LICENSE not found` warning, already documented), the bundle carries `\s`/`\d` intact and both `autoExport`/`export-failed` paths, repo hygiene grep and `preflight.sh` green (checked with `node_modules` excluded, matching what CI's hygiene job actually scans). Pushed to `main`; see diagnostics in the final message for CI confirmation on this commit.
- 2026-09-11 (scheduled routine, run 7): **Slice 2, sub-step 2 remainder: line numbers.** Confirmed run 6's CI was green in both jobs on `main` (`f1305d0`) before starting, no open Issues, no open PRs. `extension/src/frame/svg.ts`: `FrameSettings` gained `lineNumbers`; `FrameContent` gained `startLine` and `lineCount`. `buildFrameSvg` now draws a right-aligned numbered gutter (width sized from the digit count of the last line number) to the left of the code `foreignObject` when `lineNumbers` is on, leaving everything unchanged when it's off — 3 new `node:test` cases (gutter present/absent, numbering starts at the capture's start line and widens for more digits). `readFrameSettings()` reads the existing `snapframe.lineNumbers` setting (already declared in Slice 1, never wired up before now). Resolved the alignment caveat flagged in runs 5–6: `capture/source.ts`'s `CaptureSource` now also reports `rawLineCount` (the selection's raw line span); `capture/panel.ts` threads it through the `render` message, and the webview computes the actually-displayed row count itself — `rawLineCount` for the syntax-highlighted HTML path (never re-wrapped) but `fallbackText.split('\n').length` for the plain-text fallback (which goes through `dedent`/`expandTabs`/`softWrap` and can gain rows) — before sending it up in the `measured` message that feeds `buildFrameSvg`. Also fixed a real bug found while touching this file: the dims regex added in run 6 (`message.svg.match(/<svg[^>]*\swidth=.../)`) sat inside the webview's outer TS template literal, where an un-doubled `\s` is a JS "non-escape character" and is silently dropped at compile time — the regex shipped to the browser as `/<svg[^>]*swidth=.../`, which can never match real SVG output, so `lastDims` was always `null` and the Export PNG button could never appear in a real VS Code host. Doubled the backslashes (`\\s`); verified with a standalone `node -e` reproduction that the fix compiles to the intended `\s` and matches a sample `<svg ... width="493" height="332">` string. This was never caught by CI because it only surfaces when the webview JS actually runs in a browser, which this sandbox cannot do — another reason sub-step 4's golden-image suite (headless Chromium) matters. No new dependencies (`LICENSES.md` unchanged). Verified locally: `tsc --noEmit` clean, 29/29 tests pass (26 prior + 3 new), `vsce package` still produces a valid `.vsix` (same cosmetic `../LICENSE not found` warning, already documented), repo hygiene grep and `preflight.sh` green (checked with `node_modules` excluded, matching what CI's hygiene job actually scans). Pushed to `main`; see diagnostics in the final message for CI confirmation on this commit.
- 2026-09-11 (scheduled routine, run 6): **Slice 2, sub-step 2, part 2: PNG export.** Confirmed run 5's CI was green in both jobs on `main` (`d8d34c2`) before starting, no open Issues, no open PRs. Added `extension/src/export/filename.ts`: a pure `buildExportFileName(sourceFileName, startLine)` producing `snapframe-<stem>-<line>.png`, stripping only the final extension and sanitising characters unsafe in a file name — 4 new `node:test` cases. `extension/src/frame/settings.ts` gained `readExportSettings()` (scale/exportFolder/copyToClipboardAfterExport; untested by design, matching `readFrameSettings`). `capture/panel.ts`: `LastCapture` now carries `startLine`; the `'svg'` message to the webview now also carries `scale` and `copyToClipboardAfterExport`; the preview webview gained an "Export PNG" button (shown once a frame is rendered) that draws the frame SVG into an offscreen `<canvas>` via `Image`/`URL.createObjectURL` at `scale`×, reads it back with `toBlob`, and — when `copyToClipboardAfterExport` is on — first tries `navigator.clipboard.write([new ClipboardItem({'image/png': blob})])` (feature-detected; VS Code webviews are Chromium so it should work, but never assumed). The PNG bytes and the clipboard outcome are posted to the extension host, which saves via `snapframe.exportFolder` directly when set, or via `showSaveDialog` (defaulting to the built file name) when empty — matching the setting's own description, "leave empty to be asked each time" — then reports the outcome with `setStatusBarMessage` (saved; saved + copied; or saved with an explicit note that the clipboard copy wasn't supported, never a silent text-only fallback). Added `img-src ${webview.cspSource} blob: data:` to the panel's CSP so the blob-URL image used for rasterisation isn't blocked. Not done: line numbers (still nothing renders `snapframe.lineNumbers` — same alignment caveat as before, since the highlighted-HTML path still bypasses `dedent`/`expandTabs`/`softWrap`) and `snapframe.quickSnap`. The export path itself could not be exercised against a real VS Code host in this sandbox (no headless VS Code here) — that waits for the sub-step 4 golden-image suite; what's covered directly is the pure filename logic and (via the existing suite) that nothing else regressed. No new dependencies (`LICENSES.md` unchanged). Verified locally: `tsc --noEmit` clean, 26/26 tests pass (22 prior + 4 new), `vsce package` still produces a valid `.vsix` (same cosmetic `../LICENSE not found` warning, already documented), repo hygiene grep and `preflight.sh` green (the hygiene grep flags matches inside `extension/node_modules/`, which is `.gitignore`d and not what CI's hygiene job — which never runs `npm ci` — actually scans; confirmed clean with `node_modules` excluded too). Pushed to `main`; see diagnostics in the final message for CI confirmation on this commit.
- 2026-09-11 (scheduled routine, run 5): **Slice 2, sub-step 2, part 1: frame chrome in the preview.** Confirmed run 4's CI was green in both jobs on `main` (`f60db59`) before starting, no open Issues, no open PRs. Added `extension/src/frame/svg.ts`: a pure, vscode-free `buildFrameSvg(content, fileName, settings)` that lays out the exported-image chrome as a source-of-truth SVG string — background (solid colour or two-stop gradient via a `<linearGradient>`), padding, an optional `feDropShadow` under a rounded card, corner radius clamped to half the card's smaller dimension, an optional title bar (translucent strip, three macOS-style dot `<circle>`s, XML-escaped file name), and a `<foreignObject>` at the right offset holding the measured code content — 9 new `node:test` cases covering each setting combination, including a markup-injection escape check on the file name. Added `extension/src/frame/settings.ts` (`readFrameSettings()`: thin, untested-by-design read of the `snapframe.*` config already declared in Slice 1, mirroring `capture/source.ts`'s pattern). Rewired `capture/panel.ts`: the webview now measures the captured HTML (or the plain-text fallback) in a hidden `#measure` element sized with the editor's real font, posts the measured width/height/serialised-HTML back to the extension host, which calls `buildFrameSvg` with the live settings and returns the finished SVG for the webview to inline — replacing the old raw HTML dump. Settings changes while the panel is open (`workspace.onDidChangeConfiguration`, filtered to `snapframe`) now re-render the last capture live. Deliberately not done yet, left for the next sub-step: PNG rasterisation (`Image` → `canvas` → `toBlob` at 1×/2×), `showSaveDialog`, clipboard-image copy with the documented fallback-to-file behaviour, line numbers (still not rendered anywhere — the setting exists but nothing reads it), and `snapframe.quickSnap`. Also unaddressed, inherited from sub-step 1 and only relevant once line numbers are built: the syntax-highlighted HTML path bypasses `dedent`/`expandTabs`/`softWrap` (those only apply to the plain-text fallback), so a line-number gutter keyed off the normalised text's line count would misalign with the real captured HTML — whoever builds line numbers needs to either normalise the HTML too or count lines from the raw selection. No new dependencies (`LICENSES.md` unchanged). Verified locally: `tsc --noEmit` clean, 22/22 tests pass (13 prior + 9 new), `vsce package` still produces a valid `.vsix` (same cosmetic `../LICENSE not found` warning, already documented), repo hygiene grep and `preflight.sh` green. Pushed to `main`; see diagnostics in the final message for CI confirmation on this commit.
- 2026-09-11 (scheduled routine, run 4): **Slice 2, sub-step 1: the capture command.** Confirmed run 3's CI was green in both jobs on `main` (`87b8d98`) before starting, and no open Issues. Added `extension/src/text/transforms.ts` — pure, vscode-free functions `dedent` (longest common leading-whitespace prefix across non-blank lines), `expandTabs` (aligns to tab stops, not a flat replace) and `softWrap` (breaks at the last space before the column, hard-breaks a single long word) — with 9 `node:test` cases in `src/test/text.test.ts`. Added `extension/src/capture/source.ts` (`resolveCaptureSource`: selection or, when empty, the whole document; applies dedent → expandTabs (using the document's own `editor.tabSize`) → softWrap (using a new `snapframe.wrapColumn` setting, default 0 = off)) and `extension/src/capture/panel.ts` (`runCapture`: temporarily selects the full document when there is no selection, runs `editor.action.clipboardCopyWithSyntaxHighlightingAction`, restores the selection, opens a `ViewColumn.Beside` webview that pastes into a hidden contenteditable to read `text/html` off the clipboard, posts it back to the extension host, which restores the user's original clipboard text and tells the webview to render — falling back to a plain `<pre>` of the normalised text if no HTML came back). Wired `snapframe.capture` in `extension.ts` to `runCapture` (previously a "not available" stub); `quickSnap`/`enterLicence`/`buyPro` stay stubs (later sub-steps/slices). No new dependencies (`LICENSES.md` unchanged). Not unit-tested: the webview/clipboard integration itself — there is no real VS Code host in this sandbox or in `node:test`, so only the pure transforms are covered, matching what `agent/BUILD.md` sub-step 1 asks for; a real exercise of the paste-capture path waits for the Slice 2 sub-step 4 golden-image suite (headless Chromium in CI). Verified locally: `tsc --noEmit` clean, all 13 tests pass (4 prior + 9 new), `vsce package` still produces a valid `.vsix` (same cosmetic `../LICENSE not found` warning as before, already documented), repo hygiene grep and `preflight.sh` green. Pushed to `main`; see diagnostics in the final message for CI confirmation on this commit.
- 2026-09-11 (scheduled routine, run 3): **Step-0 repair, second time.** A stray upload (commit `f252d87`, pushed from the operator's account at 06:27 UTC) had put two files under `snapframe-upload/agent/`. Moved `automerge-workflow.yml` to `agent/` (no root counterpart); kept the root `agent/ROUTINE-PROMPT.md` and dropped the upload copy, exactly as step 0(b) says. **Flag for the operator:** the discarded copy was *not* identical — it added a step 4b saying this environment can only push to `claude/*` branches and that `.github/workflows/automerge.yml` must be installed so `main` is reached by fast-forward. That contradicts D12/OPERATIONS.md, and this run pushed straight to `main` twice without any rejection, so the claim is not true today. Not adopted. `agent/automerge-workflow.yml` is kept unused in case a future environment change makes it necessary; if the operator meant the new step 4b to apply, they should say so and the next run will install it. **Then Slice 1:** `extension/` skeleton — `package.json` (5 commands, 2 keybindings, context-menu entry, 12 settings, keywords, marketplace metadata, `pricing: Trial`), `src/extension.ts` (registers the commands; all but Open Settings report "not available in this preview build"), `src/licence/verify.ts` stub (`verifyKey` always `not-implemented`, `isPro()` false), `node:test` suite (4 tests: bundle builds with `vscode` external, manifest declares every command, licence stub rejects, isPro false), `scripts/generate-icon.js` (hand-encoded 256×256 PNG, no deps; output committed at `images/icon.png`), `scripts/copy-license.js` (runs before `vsce package`; `extension/LICENSE` is gitignored), `README.md` (listing text with the design's Free/Pro table, privacy statement, AI-maintained disclosure, Issues-within-a-week), `CHANGELOG.md`, `telemetry.json`, `.vscodeignore`, root `.gitignore`. Five dev deps recorded in `LICENSES.md` (all MIT/Apache-2.0, none bundled). CI: added the `extension` job (Node 22, `npm ci`, typecheck, test, package, upload `.vsix` artifact). Verified locally in this sandbox: `tsc --noEmit` clean, 4/4 tests pass, `vsce package` produces a 10 KB `.vsix` with 9 files; repo hygiene grep and `preflight.sh` green; CI on `main` was green for all prior commits. Known cosmetic: `vsce` warns "../LICENSE not found" because `package.json` keeps the design's literal `SEE LICENSE IN ../LICENSE` — the licence *is* in the `.vsix` (copied in); if the marketplace listing shows no licence at Slice 7, change the field to `SEE LICENSE IN LICENSE`. Diagnostics: `npm view esbuild version` → 0.28.2; `GET api.github.com/repos/SnapFrameCSV/snapframe` → see final message.
(Runs 1–2 summarised in `OPERATIONS.md` "Push target" and `DECISIONS.md` D12–D15; full detail in `LOG.md`.)

## Next run should
1. Check for `STOP`. If `snapframe-upload/` is back, do step 0 again and read the run-3 "Flag for the operator" note above before deciding what to keep.
2. Confirm the CI run on `main` for run 8's commit (quick snap) is green in both jobs. If the `extension` job is red, fix it first (`agent/RUN.md` §3 item 1).
3. Slice 2 (`agent/BUILD.md`), sub-step 4: the golden-image suite. Playwright headless Chromium in CI (dev dep — record `playwright`/`@playwright/test` in `LICENSES.md` first; Apache-2.0), rendering `buildFrameSvg` output for a fixed HTML fixture and comparing to stored reference PNGs within a small pixel tolerance, on Windows/macOS/Linux runners (the design's acceptance). Also verify activation time < 200 ms (the bundle is 25 KB with lazy webview creation, so mostly a measurement to record). This is the first time the webview JS runs in a real browser: the cheapest high-value start is loading `renderShell()`'s HTML in Chromium with a stubbed `acquireVsCodeApi`, feeding it a `render` + `svg` message, and asserting the `measured`/`export-png` messages come back with a decodable PNG — that exercises the paste-fallback, measure, dims-regex and rasterise paths end to end. `renderShell` is currently a module-private function in `capture/panel.ts` that imports `vscode` at the top; either export it and stub `vscode` for the test, or move the shell HTML into a `vscode`-free module (preferred: cleaner and matches how `frame/svg.ts` is kept pure). The sandbox has Chromium pre-installed at `/opt/pw-browsers/chromium` (`PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers`; do not run `playwright install` locally, but CI will need to). This closes out Slice 2.
4. If sub-step 4 cannot fit in one run, land the Linux-only Chromium smoke of the webview first (it catches the most), then the cross-OS golden compare in the next run.
5. Slices 3–5 follow in `agent/BUILD.md` order after Slice 2 is fully done. Everything customer-facing (publishing, payments, going live) still waits for gates 3–5 — no deviation from `docs/02-design.md` without a cleared gate.

## Open questions for the operator
One, answer only if it applies: **did you intend the new `agent/ROUTINE-PROMPT.md` (with step 4b, "push to your claude/* branch and let automerge.yml fast-forward main") to replace the current one?** This run kept the existing file because direct pushes to `main` still work. If you say "use the new routine prompt", the next run will install `.github/workflows/automerge.yml` from `agent/automerge-workflow.yml`, widen `ci.yml` to `claude/**` branches and switch. If you say nothing, nothing changes.

## Metrics
Not applicable yet (nothing published).
