# STATE

**Read this first on every run. Update it before every run ends.**

## Current phase
Phase 3 — Build. Slice 1 (`extension/` skeleton) landed; Slice 2 sub-step 1 (capture command, plain preview) landed; sub-step 2 (frame chrome, PNG export, line numbers) landed; sub-step 3 (`snapframe.quickSnap`) landed; sub-step 4 (golden-image suite) landed its Chromium test harness, found that the original `<foreignObject>`-based PNG export could never work in a real browser, and **the export path has now been rebuilt on native SVG text and proven to produce real PNGs in headless Chromium** (frame size, background/card/window-dot pixel colours and 1×/2× scaling all asserted). Sub-step 4 is now closed: the golden suite runs on an `ubuntu`/`windows`/`macos` CI matrix and activation is measured under 200 ms. **Slice 2 is complete. Slice 3 (offline licence verification) is complete** — `verifyKey` is real, `snapframe.enterLicence` works, `isPro()` is driven by a stored, verified key; the public key is still the gate-05 placeholder so nothing can activate yet. Slice 4 (Pro features) in progress: SVG export (item 1), WebP/PDF export + transparent background (item 2) 3×/4× scale (item 3), named presets (item 4), custom gradients / background images / caption (item 5) line highlight / focus-dim / callouts (item 6), the QR code (item 7, D16), before/after + side-by-side layouts (item 8) and terminal-selection capture (item 9) have landed. Next: item 10, batch export and "copy as Markdown image link" — the last Slice 4 item. Nothing is published; nothing customer-facing exists.

## Blocked on
Nothing.

## Last run
- 2026-09-11 (interactive continuation, same session): **Slice 4 item 10 — batch export and "copy as Markdown image link" (Pro); Slice 4 closed.** `runExportAllSelections` exports one image per non-empty selection in the active editor (multi-cursor), and `runExportAllEditors` one whole-file image per open text tab (`window.tabGroups`, de-duplicated, previous editor restored afterwards); both write straight into `snapframe.exportFolder`, asking for a folder once (`showOpenDialog`) when it is empty, and report the count. Mechanism: `startCapture` gained `BatchOptions` — the quick session records the folder override and a `done` resolver that the `export-image`/`export-failed` handlers (and panel disposal) call, so the command can `await` each capture in turn; `saveExport` takes the folder override. Markdown link: `snapframe.copyMarkdownLinkAfterExport` (Pro; `readExportSettings` forces it false without a key) copies `![<file>](<path>)` after a save — path relative to the workspace folder with forward slashes when inside one, absolute otherwise, wrapped in `<…>` when it has spaces/parentheses — but never when the image itself was just copied (the two would fight over the clipboard); pure `export/markdown.ts` with 3 tests. New `commands.ts` splits `FREE_COMMANDS` (exactly Slice 2's five) from `PRO_COMMANDS`; `commands.test.ts` is the BUILD.md "every free command works with `isPro() === false`" check made concrete: the free list is pinned verbatim, package.json must declare exactly the union, every Pro title must end in "(Pro)" and no free title may, and the free path's modules (`capture/source.ts`, `text/transforms.ts`, `webview/shell.ts`) must not reference the licence module while `runCapture`/`runQuickSnap` are asserted to call `startCapture` ungated. Two commands declared/registered. 105/105 tests, typecheck clean, no new dependencies.
- 2026-09-11 (interactive continuation, same session): **Slice 4 item 9 — terminal-selection capture (Pro).** There is no public API to read the integrated terminal's selection (`Terminal.selection` is not in the 1.137 typings), so `runCaptureTerminal` uses the one public route: save the clipboard, run `workbench.action.terminal.copySelection`, read the clipboard, restore it, and treat "clipboard unchanged" as "nothing selected". New pure `capture/terminal.ts`: `prepareTerminalText(raw, wrapColumn, tabSize = 8)` strips ANSI CSI/OSC sequences, normalises CRLF, trims trailing spaces and surrounding blank lines, expands tabs at terminal (8-column) stops and soft-wraps, returning `{ text, lineCount }` (4 tests). `startCapture` gained an optional `PlainSource` so text that did not come from an editor renders on the plain-text path: no editor required, no clipboard-HTML copy, and whatever HTML the shell's paste finds on the clipboard is ignored; the terminal's name is the card title, line numbers start at 1. The editor path was reshaped around the same `LastCapture` value (no behaviour change; 95 prior tests still green). Command `snapframe.captureTerminal` ("Capture Terminal Selection (Pro)"), Pro-gated, declared and registered. CI for the QR commit (`909b726`) was green on all four jobs. 99/99 tests.
- 2026-09-11 (interactive continuation, same session): **Slice 4 item 8 — before/after and side-by-side layouts (Pro).** `frame/svg.ts` refactored: `buildFrameSvg` is now a one-panel call to the new `buildMultiFrameSvg(panels, settings, layout)`, which lays out N `buildCard()`s (each card's shadow/clip/title bar/highlights/gutter/text/callouts drawn at its own origin, clip ids suffixed `-0`, `-1`… only when there is more than one card) in a row (`side-by-side`, 24 px gap, canvas as tall as the tallest) or a column (`stacked`), with an optional 24 px label band above each card, and the shared backdrop/gradient/image/caption/QR band around them; single-card output is byte-identical to before (asserted). `capture/panel.ts`: sessions carry a `role` (`single`/`before`/`after`); `runCaptureBefore` captures normally and parks the measured panel in `pendingBefore` with a status-bar hint; `runCaptureAfter` (refuses if nothing is pending) captures the second and renders both via `buildMultiFrameSvg` with `readLayoutSettings()` (`snapframe.layout.comparison`: side-by-side | stacked; `snapframe.layout.labels`: ["Before","After"]); the pair is kept as `lastComparison` so a settings change re-renders the comparison rather than the last single capture; any new single/before capture clears it. Both commands Pro-gated (`requirePro`), declared in `package.json` (titles end "(Pro)"), registered in `extension.ts`, manifest test updated. Export works unchanged on the combined SVG. Tests: side-by-side geometry (852×356, both clip ids, label positions, second card origin), stacked geometry (496×672, second label/card origin), one-panel equivalence + empty-panels throw (frame.test +3), and a browser export of a two-card SVG sampling card/gap/card pixels (webview-shell +1). 95/95 tests.
- 2026-09-11 (interactive continuation, same session): **Slice 4 item 7 — QR code in the image (Pro, D16).** New `frame/qr.ts`: an in-house QR encoder (byte mode, versions 1–10 → up to 213 bytes, EC level M, Reed–Solomon over GF(256), all eight masks scored by the standard penalty rules, format + version info), no dependency, returning a module matrix. Proven rather than trusted: `jsqr` (Apache-2.0, **dev-only**, recorded in `LICENSES.md`) decodes the rasterised matrix in `qr.test.ts` — six sample texts spanning versions 1 to 10 (including the 16-bit length field at v10 and an exact-capacity input), UTF-8/emoji, smallest-version selection at the 14/15-byte boundary, too-long input throws, and mask selection is live. The one bug found while writing it (format bits placed transposed around the top-left finder) was caught immediately by the decoder — exactly why the dev decoder is worth its row. `buildFrameSvg`: `pro.qr?: { text, size }` draws the code as vector squares (dark modules merged into per-row runs, `shape-rendering="crispEdges"`) on a white tile with a two-module quiet zone, right-aligned in the band under the card (the caption's band; band height = max(caption 28, qr size + 16); a right-aligned caption moves left so they never overlap); unencodable text draws nothing. Settings `snapframe.qr.text` and `snapframe.qr.size` (48–240, default 80); `readProFrameOptions()` validates the text once and warns (once per value) if it is too long. Presets gained `qr.text`/`qr.size`. README and `docs/02-design.md` §1 Pro table gained the QR row as the operator's BUILD.md note asked. 91/91 tests.
- 2026-09-11 (interactive continuation, same session): **Slice 4 item 6 — line highlight, focus-dim, callouts (Pro).** `ProFrameOptions` gained `highlightLines: number[]` (absolute file line numbers, matching the gutter), `highlightColor`, `focusDim` and `callouts: { line, text }[]`; `DEFAULT_PRO_FRAME_OPTIONS` extended with the empty/off values so the existing gate keeps collapsing everything for free users. `buildFrameSvg`: a translucent `<rect>` spanning the card behind each highlighted row (rows outside the capture ignored); with `focusDim` on and at least one highlight, every other row's `<text>` gets `opacity="0.35"`; callouts are a yellow pill (`rx=9`, width ≈ 6.5 px/char + 16, text truncated to 40 chars, XML-escaped) pinned to the right edge of the code area, vertically centred on the row, out-of-range/blank ones skipped. New pure `frame/ranges.ts`: `parseLineRanges("12-14, 20")` → sorted unique numbers, junk skipped, reversed ranges normalised, a single range capped at 10 000 lines. Settings: `snapframe.highlightLines` (string), `highlightColor`, `focusDim`, `callouts` (array of `{line,text}` with a JSON schema) — read by `readProFrameOptions()` with a tolerant `readCallouts`. Presets gained `highlightColor`/`focusDim` (`highlightLines`/`callouts` are per-snippet and deliberately not preset-able). Tests: ranges (3), highlight band placement + out-of-range, focus-dim on/off (the assertion had to exclude the shadow's `flood-opacity="0.35"`), callout geometry/escape/truncate/skip, gate defaults (frame.test +3, formats.test updated, presets key list). 86/86 tests.
- 2026-09-11 (interactive continuation, same session): **Slice 4 item 5 — custom gradients, background image, caption/brand colour (Pro).** `FrameSettings` gained `pro: ProFrameOptions` (`gradientAngle`, `gradientStops`, `backgroundImage?` data URI, `caption? {text,color,position}`) with `DEFAULT_PRO_FRAME_OPTIONS = { gradientAngle: 135, gradientStops: [] }` — chosen so the free tier's output is byte-identical to before: new exported `gradientVector(angle)` maps a CSS-style angle onto edge-to-edge objectBoundingBox coordinates and 135° yields exactly the old `(0,0)→(1,1)`. Multi-stop gradients space stops evenly; a `backgroundImage` is drawn as `<image preserveAspectRatio="xMidYMid slice">` over the backdrop but only when the href is a `data:image/` URI (anything else is ignored — the SVG stays self-contained and a `file:` URL would taint the canvas); a non-blank caption adds a 28 px band under the card with the text at left/centre/right in the caption colour, XML-escaped. Settings (all described as Pro): `background.gradientAngle`, `background.gradientStops` (≤ 8), `background.image` (absolute path), `caption.text/color/position`. `readProFrameOptions()` (async, `frame/settings.ts`) reads them, loads the image file via `workspace.fs` and embeds it as base64 (png/jpg/webp/gif by extension, 5 MB cap, warnings not errors on problems), then runs everything through the pure `allowedProFrameOptions(requested, pro, freeDefaults)` gate — without a key the extras collapse to the free defaults regardless of what is configured. `capture/panel.ts` awaits it when building the SVG. Presets gained the five new keys (validated; `background.image` deliberately excluded as a machine-local path). Tests: `gradientVector` mapping, multi-stop/angle, data-URI-only image, caption band/position/escape/blank (frame.test.ts +5), the gate (formats.test.ts +1), presets key list, and a browser test exporting a frame with a 1×1 PNG data-URI background + caption and sampling the corner pixel blue (no taint). 80/80 tests.
- 2026-09-11 (interactive continuation, same session): **Slice 4 item 4 — named presets (Pro).** Pure `presets/presets.ts`: a preset is `{ name, settings }` where `settings` holds only the eleven preset-able `snapframe.*` keys (background type/colour/gradient, padding, shadow, cornerRadius, windowControls, lineNumbers, titleBar, wrapColumn, scale), each run through a validator table (`sanitiseSettings` drops unknown keys, wrong types, out-of-range numbers and non-colour strings — so an imported file can only ever set those keys to well-shaped values; `exportFolder` is deliberately not preset-able). `serialisePresets` writes `{ version: 1, presets }`; `parsePresets` accepts that or a bare array, never throws, skips junk entries, trims/validates names (≤ 60 chars, single line), de-duplicates by name and returns a plain-English `error` when nothing usable is found; `upsertPreset` (exact-name replace, order kept), `removePreset`, `mergePresets` (imported wins; counts added/replaced). `presets/commands.ts`: five commands, each `requirePro`-gated with the standard "Pro feature" message — `savePreset` (input box → snapshot of current settings → globalState `snapframe.presets`), `applyPreset` (quick pick with a one-line summary → `config.update(key, value, Global)` per key), `deletePreset`, `exportPresets` (save dialog → JSON), `importPresets` (open dialog → parse → merge → "imported N new, updated M"). globalState reads are re-sanitised on load. `package.json` declares the five commands (titles end in "(Pro)"); `extension.ts` registers them; `smoke.test.ts`'s manifest list updated. `presets.test.ts` (8): round trip, key list pinned to real setting names, sanitise drops junk (incl. a `__proto__` key and an `exportFolder` smuggle), bare-array/junk/dedupe parsing, error messages, upsert/remove/merge semantics, name rules. 74/74 tests.
- 2026-09-11 (interactive continuation, same session): **Slice 4 items 2 and 3 — WebP/PDF export, transparent background, 3×/4× scale (all Pro).** CI for the first three-OS matrix run (`8f80bcb`) was green on all legs before this landed. New pure `export/pdf.ts`: `buildPdf()` writes a single-page PDF-1.4 with one FlateDecode DeviceRGB image XObject (plus a DeviceGray `/SMask` when an alpha plane is given), page size in points = the frame's CSS-pixel size so higher scales are sharper not bigger, exact xref offsets, no dependencies (node `zlib`); `splitRgba()` separates canvas pixels into RGB + alpha and reports opacity. `pdf.test.ts` (5) parses the output: header, MediaBox, content stream, every xref offset lands on its `N 0 obj`, the image stream inflates back to the exact pixels, SMask only with alpha, dimension mismatches throw. `webview/shell.ts`: `exportPng` became `exportRaster(format)` — PNG/WebP via `canvas.toBlob` (the WebP path checks `blob.type` and fails loudly if the browser silently produced PNG), PDF via `getImageData` posted as `export-pixels` (base64 RGBA, chunked `btoa`) for the host to write; three `data-pro` toolbar buttons (SVG/WebP/PDF) shown only for Pro; all buttons disable during an export; clipboard copy still PNG-only. `capture/panel.ts`: `export-png` became `export-image {format}` (png/webp only), new `export-pixels` handler → `buildPdf` (SMask only if not fully opaque) → `saveExport(…, 'pdf')`. Transparent background: `snapframe.background.type` enum gained `transparent` (with `enumDescriptions`); `FrameSettings.backgroundType` too; `buildFrameSvg` omits the backdrop rect entirely for it (the card and shadow still draw), so PNG/WebP keep alpha and the PDF gets an SMask; `readFrameSettings()` runs the value through the new pure `allowedBackgroundType(type, pro)` (falls back to `solid`). Scale: `snapframe.scale` enum is now `[1,2,3,4]` and `readExportSettings()` runs it through `allowedScale` (3/4 fall back to 2 without a key) — this is BUILD.md item 3, folded into this commit because the same `settings.ts` gate wiring carried both. Browser tests: Pro buttons hidden for free/shown for Pro; WebP click yields RIFF/WEBP bytes; PDF click on a transparent frame at 2× posts pixels of exactly 2× the SVG size with a zero-alpha corner and an opaque card, which `buildPdf` accepts. 66/66 tests.
- 2026-09-11 (interactive continuation, same session): **Slice 4 item 1 — SVG export (Pro).** New pure `export/formats.ts`: `FREE_FORMATS = ['png']`, `PRO_FORMATS = ['svg','webp','pdf']`, `FREE_SCALES = [1,2]`, `PRO_SCALES = [3,4]`, `isFormatAllowed(format, pro)`, `allowedScale(requested, pro)` (Pro scales fall back to 2×), `FORMAT_LABELS`. `formats.test.ts` (5) is the D05 contract: the free sets are asserted verbatim so they can only widen, free/Pro never overlap, Pro formats/scales refused without a licence and honoured with one. `buildExportFileName` takes the extension. `capture/panel.ts`: `exportPng` generalised to `saveExport(capture, bytes, format, clipboard)` — per-format save-dialog filter and status text, and it re-checks `isFormatAllowed(format, isPro())` itself so a stray webview message can't bypass the UI gate; the session keeps the last built `svg`; the `svg` message carries `pro: isPro()`; new `export-svg` message saves the frame as a standalone `.svg` (XML declaration prepended). `webview/shell.ts`: toolbar gained an "Export SVG" button shown only when `pro && lastDims && !quick`; click posts `export-svg` (the host already holds the SVG). Browser test added: free → PNG button only; Pro → both buttons; clicking SVG posts `export-svg`. The existing full-pipeline PNG test runs with `pro: false`, which is the "free tier still works with `isPro() === false`" check BUILD.md asks for at the export level. 58/58 tests. WebP/PDF scales etc. are still to come; `isPro()` is false for everyone until gate-05, so none of this is reachable yet.
- 2026-09-11 (interactive continuation, same session): **Slice 3 — offline licence verification.** `licence/verify.ts`: `verifyKey(key, publicKeyRaw32, nowSeconds?)` parses `SNAP-<base64url payload>.<base64url 64-byte signature>` (whitespace-trimmed; strict base64url; exactly one dot), verifies the Ed25519 signature with Node `crypto.verify(null, …)` against an SPKI built from the raw 32-byte key (DER prefix `302a300506032b6570032100`), and only *then* parses/validates the payload (`{p:'pro', v:int, iat:number, e:12 hex}`) and refuses an `iat` more than `FUTURE_TOLERANCE_SECONDS` (24 h clock-skew allowance) ahead. Failure reasons: `malformed` / `no-public-key` / `bad-signature` / `issued-in-future`, each with a plain-English `describeFailure()` that never mentions a server. The module also owns the session's Pro state (`isPro()`, `activeLicencePayload()`, `setActiveLicence()`) so Slice 4 gates and pure tests can use it without vscode. `licence/publicKey.ts`: `LICENCE_PUBLIC_KEY_HEX = 'REPLACE_AT_GATE_05'` and `licencePublicKey()` (null until 64 hex chars are pasted in). `licence/activate.ts`: `initialiseLicence(context)` creates the "Snapframe Pro" status-bar item, reads the key from `context.secrets` (`snapframe.licenceKey`), verifies and shows/hides accordingly, and re-applies on `secrets.onDidChange` (Settings Sync); `enterLicence(context)` is the `snapframe.enterLicence` command — input box with `SNAP-` validation → verify → `secrets.store` → status bar → confirmation; a bad key gets the plain-English reason; while the public key is the placeholder it says Pro keys can't be activated in this preview build. `extension.ts` wires it (not awaited, so activation never waits on SecretStorage); `buyPro` stays a stub until the Worker's `/buy` exists (Slice 5). Tests (`licence.test.ts`, 12): a per-run generated Ed25519 pair mints keys; valid key, whitespace, tampered payload, other private key, wrong public key, ten malformed strings, correctly-signed-but-not-a-licence payloads, future-`iat` tolerance boundary, wrong-size public key, placeholder public key is still null, `isPro` follows `setActiveLicence`, failure descriptions mention no server. `smoke.test.ts`'s vscode stub gained `createStatusBarItem`/`StatusBarAlignment`/`secrets`. README preview note and CHANGELOG updated. 51/51 tests, typecheck clean, no new dependencies.
- 2026-09-11 (interactive continuation, same session): **Slice 2 sub-step 4 closed.** (a) Cross-OS golden compare: the `extension` CI job is now a `fail-fast: false` matrix over `ubuntu-latest`, `windows-latest`, `macos-latest`; every OS runs `npm ci` → `playwright install --with-deps chromium` → typecheck → the full suite including the browser/golden tests; packaging and the `.vsix` upload stay Linux-only. Chosen over stored reference PNGs deliberately: the frame is native SVG now, so the only cross-OS variable is font rasterisation, and the suite already checks the chrome pixels exactly (frame colour, card colour, window dot) and the text rows by presence — a stored-PNG diff would only add a fragile font-dependent comparison and a binary in the repo. (b) Activation time: `smoke.test.ts` gained a test that builds the bundle, loads it with a stubbed `vscode` module (hooking `Module._load`, the way the extension host injects it), times `require` + `activate()` and asserts < 200 ms (measured well under 10 ms here — `activate()` only registers five commands and builds nothing), and cross-checks the registered command ids against the manifest. 41/41 tests. Design §2's "golden-image tests (headless Chromium)" and Slice 2's acceptance line are met; the design's fallback ("if golden tests can't be made reliable across fonts, ship SVG as the reference") is not needed.
- 2026-09-11 (interactive continuation of run 9, same session — the operator said "keep running through the upgrades"): **Export PNG fixed; Slice 2 sub-step 4 golden suite now proves a real export.** `frame/svg.ts` no longer emits a `<foreignObject>`: `FrameContent` is now `{ lines: TextRun[][], width, height, startLine, lineCount, fontFamily, fontSize, color, background }` where each `TextRun` is `{ text, color?, bold?, italic? }` carrying only what differs from the wrapper defaults; `buildFrameSvg` draws one `<text xml:space="preserve" dominant-baseline="central">` per row (centred on the row, rows = `height / lineCount`, same math as the gutter) with one `<tspan>` per run (consecutive tspans without x/y flow inline, so a row lays out like inline HTML spans; no whitespace is emitted between tags because `xml:space="preserve"` would render it). The card is now filled with the *captured* editor background (`content.background`) instead of a hard-coded `#1e1e1e`, and a new exported `CODE_PADDING = 16` sits between card edge and text (the old `#measure` padding moved here, so measured `width`/`height` are the bare text box — every size in `frame.test.ts` changed accordingly). `webview/shell.ts`: `renderCapture` now reads the resolved font-family/size, colour and background from the capture's wrapper element (VS Code's copy-with-highlighting sets them inline) or, for the plain-text fallback, from `#measure` (which now carries `--vscode-editor-foreground`/`-background` defaults so fallback text is never black-on-dark), then `extractLines()` walks the measured DOM into rows of runs: block boundaries (`<div>` per line, `<br>` for an empty one, contenteditable-style) and literal `\n` (the fallback `<pre>`) start rows; each text node's style is resolved with `getComputedStyle` so nested/inherited styles come out right; adjacent same-style runs are merged; rows are trimmed of a trailing phantom `<div><br></div>` and padded to `lineCount`. `capture/panel.ts` just passes the new `measured` fields through. Tests: `frame.test.ts` rewritten for the new shape (16 tests, was 11 — new ones cover tspans-per-run, whitespace/markup escaping in runs, blank rows keeping later rows on their baseline, captured background); `webview-shell.test.ts` rewritten from the KNOWN BUG pins into the real golden suite (5 tests): fallback measure + run extraction, a VS Code-style highlighted fixture (3 lines incl. an empty `<div><br></div>` and an italic comment) extracted into exactly the expected runs with wrapper defaults stripped, the **full pipeline** (measure → `buildFrameSvg` → `svg` message → click → `export-png`) producing a PNG whose IHDR size equals the SVG's at 1× and 2× and whose sampled pixels are the solid frame colour, the captured card colour, the first window dot's colour, and non-card pixels in the first code row (text drawn); quick-snap auto-export; and the no-dimensions `export-failed` path. No new dependencies. Verified locally: `tsc --noEmit` clean, 40/40 tests pass, `vsce package` produces a valid `.vsix` (9 files), hygiene grep and `preflight.sh` green. Pushed to `main`.
- 2026-09-11 (scheduled routine, run 9): **Slice 2, sub-step 4 (golden-image suite), part 1 — and a critical finding.** Confirmed run 8's CI was green in both jobs on `main` (`a62531a`) before starting, no open Issues, no open PRs. Extracted the webview's HTML/CSS/JS shell out of `capture/panel.ts` into a pure, `vscode`-free `webview/shell.ts` (`renderShell(cspSource, quick)`, no behaviour change — `panel.ts` now just calls it with `session.panel.webview.cspSource`) so it can be loaded directly in a real browser. Added `playwright` (Apache-2.0, dev-only, recorded in `LICENSES.md`) and `extension/src/test/webview-shell.test.ts`: launches headless Chromium (the sandbox's pre-installed build at `/opt/pw-browsers/chromium` when present, else Playwright's own resolution — CI installs its own via a new `npx playwright install --with-deps chromium` step in `ci.yml`), serves the real shell HTML from a routed `https://` URL (not `page.setContent`/`data:`, which either don't reliably fire `addInitScript` or introduce an opaque-origin confound — see the test file's comments), stubs `acquireVsCodeApi` to capture `postMessage` calls, and drives it exactly as `capture/panel.ts` does: paste-fallback on load, a `render` message, an `svg` message. This is the first time the webview's `<script>` has ever executed in a real browser (the sandbox and `node:test` can't run browser JS at all).

  **Found in the process: Export PNG does not work in a real browser.** Clicking Export (or quickSnap's auto-export) throws `Failed to execute 'toBlob' on 'HTMLCanvasElement': Tainted canvases may not be exported.` Root cause, isolated with a minimal standalone repro (a bare SVG with vs. without a `<foreignObject>`, rasterised via `Image`→`canvas`→`toBlob`, no CSP, same origin): per the Canvas/SVG spec, rasterising *any* image whose SVG source contains a `<foreignObject>` descendant unconditionally taints the canvas — not an origin/CORS/CSP check, no opt-out. `buildFrameSvg` embeds the entire code block in a `<foreignObject>`, which is exactly the rasterised image source both the preview's Export button and `quickSnap` draw to canvas. This is browser hardening from ~2023, not new code here, so it isn't sandbox-specific — a real VS Code webview (Electron's bundled, evergreen Chromium) almost certainly hits the same wall. Every run since 5 that reported PNG export or quickSnap as "done" could only verify `buildFrameSvg`'s string output and the filename/settings logic; nothing here or in CI ran the webview JS in a browser until this run, which is precisely why sub-step 4 exists. Not a regression from anything this run touched.

  Rather than land a false-green test, `webview-shell.test.ts` pins the *current, broken* behaviour: two tests cover what genuinely works today (paste-fallback → measure round-trip; an `svg` message revealing the Export button via the dims regex), and two are explicitly named `KNOWN BUG` and assert `export-failed` with the exact tainted-canvas message, so they'll force a visible, intentional change (not a silent flip) when the real fix lands. Fix direction is scoped in the test file's doc comment and repeated in "Next run should" below: stop using `foreignObject` as the *rasterised* image source — it can stay for the live inline-SVG preview (display-only, never hits this) — and instead build the code block for export as native SVG `<text>`/`<tspan>` runs (consecutive tspans with no `x`/`y` flow inline automatically, matching HTML layout; `buildLineNumbers` already proves this technique for the gutter and does not taint).

  Cross-OS golden pixel-diff against stored reference PNGs (the design's full sub-step 4 acceptance criterion) and the activation-time measurement are both still undone — there is nothing meaningful to pixel-compare while export cannot produce an image, so they wait for the fix.

  No product code changed (no risk to shipped behaviour — nothing is shipped). Verified locally: `tsc --noEmit` clean, 35/35 tests pass (31 prior + 4 new), `vsce package` still produces a valid `.vsix` (9 files, same cosmetic `../LICENSE not found` warning), repo hygiene grep and `preflight.sh` green. Pushed to `main`; see diagnostics in the final message for CI confirmation on this commit (CI now takes longer — installs Chromium).
- 2026-09-11 (scheduled routine, run 8): **Slice 2, sub-step 3: `snapframe.quickSnap`.** Confirmed run 7's CI was green in both jobs on `main` (`45ed463`) before starting, no open Issues, no open PRs. Approach decided and documented (in the `runQuickSnap` doc comment): VS Code has no offscreen webview API, the extension host has no `Image`/`canvas`, and the clipboard-HTML capture itself already needs a *focused* webview for its paste event — so "no panel" cannot mean "no webview". Quick Snap therefore runs the identical pipeline (paste-capture → measure → `buildFrameSvg` → rasterise) inside a throwaway panel that auto-exports the moment its SVG arrives (the host sends `autoExport: true` on the `svg` message), then the host disposes the panel *before* any save dialog so it is on screen as briefly as possible, saves via the same `exportPng` path (`exportFolder` or `showSaveDialog`, same status-bar outcomes), and hands focus back to the original editor/selection via `showTextDocument`. "Last settings" = the live `snapframe.*` configuration, which is what the preview persists to. The open preview panel (if any) is never touched. `capture/panel.ts` refactored from module-level `activePanel`/`lastCapture` singletons to a per-webview `Session` object (`{ panel, quick, capture, returnTo }`) so a preview session and a quick-snap session can coexist; the preview remains the singleton that re-renders on settings changes. Webview also gained an `export-failed` message (host shows an error and, in quick mode, closes the panel and returns focus) instead of only writing the reason into the panel's status line. `extension.ts` wires `snapframe.quickSnap` to `runQuickSnap` (off its "not available" stub); `enterLicence`/`buyPro` stay stubs. Also acted on run 7's request to audit the webview `<script>` for other silently-eaten single backslashes: found none by hand, then added `src/test/webview-escapes.test.ts` — a source-level `node:test` that walks every backslash in the inline script block and fails on any that is not doubled (or an outer-literal `` \` ``/`\$` escape), plus a positive check that the dims regex and the `'\\n'` split are doubled. Mutation-checked: un-doubling the dims regex makes both tests fail. `CHANGELOG.md` 0.0.1 entry and the README "preview build" note updated to stop claiming nothing captures yet (0.0.1 is unreleased, so nothing customer-facing changed). Not unit-testable here, same as runs 4–7: the webview/dispose/focus flow needs a real VS Code host — that is what sub-step 4's headless-Chromium suite starts to cover for the webview half. No new dependencies (`LICENSES.md` unchanged). Verified locally: `tsc --noEmit` clean, 31/31 tests pass (29 prior + 2 new), `vsce package` still produces a valid `.vsix` (9 files, same cosmetic `../LICENSE not found` warning, already documented), the bundle carries `\s`/`\d` intact and both `autoExport`/`export-failed` paths, repo hygiene grep and `preflight.sh` green (checked with `node_modules` excluded, matching what CI's hygiene job actually scans). Pushed to `main`; see diagnostics in the final message for CI confirmation on this commit.
- 2026-09-11 (scheduled routine, run 7): **Slice 2, sub-step 2 remainder: line numbers.** Confirmed run 6's CI was green in both jobs on `main` (`f1305d0`) before starting, no open Issues, no open PRs. `extension/src/frame/svg.ts`: `FrameSettings` gained `lineNumbers`; `FrameContent` gained `startLine` and `lineCount`. `buildFrameSvg` now draws a right-aligned numbered gutter (width sized from the digit count of the last line number) to the left of the code `foreignObject` when `lineNumbers` is on, leaving everything unchanged when it's off — 3 new `node:test` cases (gutter present/absent, numbering starts at the capture's start line and widens for more digits). `readFrameSettings()` reads the existing `snapframe.lineNumbers` setting (already declared in Slice 1, never wired up before now). Resolved the alignment caveat flagged in runs 5–6: `capture/source.ts`'s `CaptureSource` now also reports `rawLineCount` (the selection's raw line span); `capture/panel.ts` threads it through the `render` message, and the webview computes the actually-displayed row count itself — `rawLineCount` for the syntax-highlighted HTML path (never re-wrapped) but `fallbackText.split('\n').length` for the plain-text fallback (which goes through `dedent`/`expandTabs`/`softWrap` and can gain rows) — before sending it up in the `measured` message that feeds `buildFrameSvg`. Also fixed a real bug found while touching this file: the dims regex added in run 6 (`message.svg.match(/<svg[^>]*\swidth=.../)`) sat inside the webview's outer TS template literal, where an un-doubled `\s` is a JS "non-escape character" and is silently dropped at compile time — the regex shipped to the browser as `/<svg[^>]*swidth=.../`, which can never match real SVG output, so `lastDims` was always `null` and the Export PNG button could never appear in a real VS Code host. Doubled the backslashes (`\\s`); verified with a standalone `node -e` reproduction that the fix compiles to the intended `\s` and matches a sample `<svg ... width="493" height="332">` string. This was never caught by CI because it only surfaces when the webview JS actually runs in a browser, which this sandbox cannot do — another reason sub-step 4's golden-image suite (headless Chromium) matters. No new dependencies (`LICENSES.md` unchanged). Verified locally: `tsc --noEmit` clean, 29/29 tests pass (26 prior + 3 new), `vsce package` still produces a valid `.vsix` (same cosmetic `../LICENSE not found` warning, already documented), repo hygiene grep and `preflight.sh` green (checked with `node_modules` excluded, matching what CI's hygiene job actually scans). Pushed to `main`; see diagnostics in the final message for CI confirmation on this commit.
- 2026-09-11 (scheduled routine, run 6): **Slice 2, sub-step 2, part 2: PNG export.** Confirmed run 5's CI was green in both jobs on `main` (`d8d34c2`) before starting, no open Issues, no open PRs. Added `extension/src/export/filename.ts`: a pure `buildExportFileName(sourceFileName, startLine)` producing `snapframe-<stem>-<line>.png`, stripping only the final extension and sanitising characters unsafe in a file name — 4 new `node:test` cases. `extension/src/frame/settings.ts` gained `readExportSettings()` (scale/exportFolder/copyToClipboardAfterExport; untested by design, matching `readFrameSettings`). `capture/panel.ts`: `LastCapture` now carries `startLine`; the `'svg'` message to the webview now also carries `scale` and `copyToClipboardAfterExport`; the preview webview gained an "Export PNG" button (shown once a frame is rendered) that draws the frame SVG into an offscreen `<canvas>` via `Image`/`URL.createObjectURL` at `scale`×, reads it back with `toBlob`, and — when `copyToClipboardAfterExport` is on — first tries `navigator.clipboard.write([new ClipboardItem({'image/png': blob})])` (feature-detected; VS Code webviews are Chromium so it should work, but never assumed). The PNG bytes and the clipboard outcome are posted to the extension host, which saves via `snapframe.exportFolder` directly when set, or via `showSaveDialog` (defaulting to the built file name) when empty — matching the setting's own description, "leave empty to be asked each time" — then reports the outcome with `setStatusBarMessage` (saved; saved + copied; or saved with an explicit note that the clipboard copy wasn't supported, never a silent text-only fallback). Added `img-src ${webview.cspSource} blob: data:` to the panel's CSP so the blob-URL image used for rasterisation isn't blocked. Not done: line numbers (still nothing renders `snapframe.lineNumbers` — same alignment caveat as before, since the highlighted-HTML path still bypasses `dedent`/`expandTabs`/`softWrap`) and `snapframe.quickSnap`. The export path itself could not be exercised against a real VS Code host in this sandbox (no headless VS Code here) — that waits for the sub-step 4 golden-image suite; what's covered directly is the pure filename logic and (via the existing suite) that nothing else regressed. No new dependencies (`LICENSES.md` unchanged). Verified locally: `tsc --noEmit` clean, 26/26 tests pass (22 prior + 4 new), `vsce package` still produces a valid `.vsix` (same cosmetic `../LICENSE not found` warning, already documented), repo hygiene grep and `preflight.sh` green (the hygiene grep flags matches inside `extension/node_modules/`, which is `.gitignore`d and not what CI's hygiene job — which never runs `npm ci` — actually scans; confirmed clean with `node_modules` excluded too). Pushed to `main`; see diagnostics in the final message for CI confirmation on this commit.
- 2026-09-11 (scheduled routine, run 5): **Slice 2, sub-step 2, part 1: frame chrome in the preview.** Confirmed run 4's CI was green in both jobs on `main` (`f60db59`) before starting, no open Issues, no open PRs. Added `extension/src/frame/svg.ts`: a pure, vscode-free `buildFrameSvg(content, fileName, settings)` that lays out the exported-image chrome as a source-of-truth SVG string — background (solid colour or two-stop gradient via a `<linearGradient>`), padding, an optional `feDropShadow` under a rounded card, corner radius clamped to half the card's smaller dimension, an optional title bar (translucent strip, three macOS-style dot `<circle>`s, XML-escaped file name), and a `<foreignObject>` at the right offset holding the measured code content — 9 new `node:test` cases covering each setting combination, including a markup-injection escape check on the file name. Added `extension/src/frame/settings.ts` (`readFrameSettings()`: thin, untested-by-design read of the `snapframe.*` config already declared in Slice 1, mirroring `capture/source.ts`'s pattern). Rewired `capture/panel.ts`: the webview now measures the captured HTML (or the plain-text fallback) in a hidden `#measure` element sized with the editor's real font, posts the measured width/height/serialised-HTML back to the extension host, which calls `buildFrameSvg` with the live settings and returns the finished SVG for the webview to inline — replacing the old raw HTML dump. Settings changes while the panel is open (`workspace.onDidChangeConfiguration`, filtered to `snapframe`) now re-render the last capture live. Deliberately not done yet, left for the next sub-step: PNG rasterisation (`Image` → `canvas` → `toBlob` at 1×/2×), `showSaveDialog`, clipboard-image copy with the documented fallback-to-file behaviour, line numbers (still not rendered anywhere — the setting exists but nothing reads it), and `snapframe.quickSnap`. Also unaddressed, inherited from sub-step 1 and only relevant once line numbers are built: the syntax-highlighted HTML path bypasses `dedent`/`expandTabs`/`softWrap` (those only apply to the plain-text fallback), so a line-number gutter keyed off the normalised text's line count would misalign with the real captured HTML — whoever builds line numbers needs to either normalise the HTML too or count lines from the raw selection. No new dependencies (`LICENSES.md` unchanged). Verified locally: `tsc --noEmit` clean, 22/22 tests pass (13 prior + 9 new), `vsce package` still produces a valid `.vsix` (same cosmetic `../LICENSE not found` warning, already documented), repo hygiene grep and `preflight.sh` green. Pushed to `main`; see diagnostics in the final message for CI confirmation on this commit.
- 2026-09-11 (scheduled routine, run 4): **Slice 2, sub-step 1: the capture command.** Confirmed run 3's CI was green in both jobs on `main` (`87b8d98`) before starting, and no open Issues. Added `extension/src/text/transforms.ts` — pure, vscode-free functions `dedent` (longest common leading-whitespace prefix across non-blank lines), `expandTabs` (aligns to tab stops, not a flat replace) and `softWrap` (breaks at the last space before the column, hard-breaks a single long word) — with 9 `node:test` cases in `src/test/text.test.ts`. Added `extension/src/capture/source.ts` (`resolveCaptureSource`: selection or, when empty, the whole document; applies dedent → expandTabs (using the document's own `editor.tabSize`) → softWrap (using a new `snapframe.wrapColumn` setting, default 0 = off)) and `extension/src/capture/panel.ts` (`runCapture`: temporarily selects the full document when there is no selection, runs `editor.action.clipboardCopyWithSyntaxHighlightingAction`, restores the selection, opens a `ViewColumn.Beside` webview that pastes into a hidden contenteditable to read `text/html` off the clipboard, posts it back to the extension host, which restores the user's original clipboard text and tells the webview to render — falling back to a plain `<pre>` of the normalised text if no HTML came back). Wired `snapframe.capture` in `extension.ts` to `runCapture` (previously a "not available" stub); `quickSnap`/`enterLicence`/`buyPro` stay stubs (later sub-steps/slices). No new dependencies (`LICENSES.md` unchanged). Not unit-tested: the webview/clipboard integration itself — there is no real VS Code host in this sandbox or in `node:test`, so only the pure transforms are covered, matching what `agent/BUILD.md` sub-step 1 asks for; a real exercise of the paste-capture path waits for the Slice 2 sub-step 4 golden-image suite (headless Chromium in CI). Verified locally: `tsc --noEmit` clean, all 13 tests pass (4 prior + 9 new), `vsce package` still produces a valid `.vsix` (same cosmetic `../LICENSE not found` warning as before, already documented), repo hygiene grep and `preflight.sh` green. Pushed to `main`; see diagnostics in the final message for CI confirmation on this commit.
- 2026-09-11 (scheduled routine, run 3): **Step-0 repair, second time.** A stray upload (commit `f252d87`, pushed from the operator's account at 06:27 UTC) had put two files under `snapframe-upload/agent/`. Moved `automerge-workflow.yml` to `agent/` (no root counterpart); kept the root `agent/ROUTINE-PROMPT.md` and dropped the upload copy, exactly as step 0(b) says. **Flag for the operator:** the discarded copy was *not* identical — it added a step 4b saying this environment can only push to `claude/*` branches and that `.github/workflows/automerge.yml` must be installed so `main` is reached by fast-forward. That contradicts D12/OPERATIONS.md, and this run pushed straight to `main` twice without any rejection, so the claim is not true today. Not adopted. `agent/automerge-workflow.yml` is kept unused in case a future environment change makes it necessary; if the operator meant the new step 4b to apply, they should say so and the next run will install it. **Then Slice 1:** `extension/` skeleton — `package.json` (5 commands, 2 keybindings, context-menu entry, 12 settings, keywords, marketplace metadata, `pricing: Trial`), `src/extension.ts` (registers the commands; all but Open Settings report "not available in this preview build"), `src/licence/verify.ts` stub (`verifyKey` always `not-implemented`, `isPro()` false), `node:test` suite (4 tests: bundle builds with `vscode` external, manifest declares every command, licence stub rejects, isPro false), `scripts/generate-icon.js` (hand-encoded 256×256 PNG, no deps; output committed at `images/icon.png`), `scripts/copy-license.js` (runs before `vsce package`; `extension/LICENSE` is gitignored), `README.md` (listing text with the design's Free/Pro table, privacy statement, AI-maintained disclosure, Issues-within-a-week), `CHANGELOG.md`, `telemetry.json`, `.vscodeignore`, root `.gitignore`. Five dev deps recorded in `LICENSES.md` (all MIT/Apache-2.0, none bundled). CI: added the `extension` job (Node 22, `npm ci`, typecheck, test, package, upload `.vsix` artifact). Verified locally in this sandbox: `tsc --noEmit` clean, 4/4 tests pass, `vsce package` produces a 10 KB `.vsix` with 9 files; repo hygiene grep and `preflight.sh` green; CI on `main` was green for all prior commits. Known cosmetic: `vsce` warns "../LICENSE not found" because `package.json` keeps the design's literal `SEE LICENSE IN ../LICENSE` — the licence *is* in the `.vsix` (copied in); if the marketplace listing shows no licence at Slice 7, change the field to `SEE LICENSE IN LICENSE`. Diagnostics: `npm view esbuild version` → 0.28.2; `GET api.github.com/repos/SnapFrameCSV/snapframe` → see final message.
(Runs 1–2 summarised in `OPERATIONS.md` "Push target" and `DECISIONS.md` D12–D15; full detail in `LOG.md`.)

## Next run should
1. Check for `STOP`. If `snapframe-upload/` is back, do step 0 again and read the run-3 "Flag for the operator" note above before deciding what to keep.
2. Confirm CI on `main` for the latest commit is green in all four jobs (hygiene + the three-OS extension matrix). The Windows and macOS legs are new: if one is red because of a runner/Playwright install problem rather than a test, fix the workflow; if a *test* is red only on one OS, it is a real cross-OS rendering difference — investigate before touching the assertion.
3. Continue `agent/BUILD.md` in order from wherever "Current phase" says. Known limitation to carry into the README's "known issues" or fix if cheap: a tab character inside a highlighted run renders as a single space in SVG (`xml:space="preserve"` collapses tabs); VS Code's copy already expands tabs to spaces, and the fallback path runs `expandTabs`, so it should not occur in practice.
5. Slices 3–5 follow in `agent/BUILD.md` order after Slice 2 is fully done. Everything customer-facing (publishing, payments, going live) still waits for gates 3–5 — no deviation from `docs/02-design.md` without a cleared gate.

## Open questions for the operator
One, answer only if it applies: **did you intend the new `agent/ROUTINE-PROMPT.md` (with step 4b, "push to your claude/* branch and let automerge.yml fast-forward main") to replace the current one?** This run kept the existing file because direct pushes to `main` still work. If you say "use the new routine prompt", the next run will install `.github/workflows/automerge.yml` from `agent/automerge-workflow.yml`, widen `ci.yml` to `claude/**` branches and switch. If you say nothing, nothing changes.

## Metrics
Not applicable yet (nothing published).
