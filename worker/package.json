{
  "name": "snapframe-licence-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that issues Snapframe Pro licence keys after a Stripe purchase.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "pretest": "tsc -p tsconfig.json",
    "test": "node --test \"out/test/**/*.test.js\""
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
